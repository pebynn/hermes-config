# R9-B 实施计划 — MAX_POSITIONS 6→8

**Task**: t_e59d1635  
**Target**: /home/pebynn/quant/evo_optimizer/strategy_reversal.py  
**Date**: 2026-05-13  
**Parent**: R8-B (t_2002f2b0)

---

## 设计方案

### 方案选择
**唯一变更**: MAX_POSITIONS: 6 → 8 (其余所有参数完全保持 R8-B 不变)

影响范围（代码中引用 MAX_POSITIONS 的位置）:
1. `line 67`: 常量定义 `MAX_POSITIONS = 6` → `8`
2. `line 10` (docstring): `MAX_POSITIONS RESTORED 6` → 更新注释
3. `line 33` (docstring): `nlargest(MAX_POSITIONS=6, 'score')` → 更新注释
4. `line 587`: `min(MAX_POSITIONS, len(candidates))` — 自动适配，无需修改
5. `line 1040`: `slots_needed = MAX_POSITIONS - len(bt.portfolio)` — 自动适配，无需修改

### R8-B 基线（待超越）
| 指标 | R8-B | R5 (MAX_POS=8) | 目标 |
|:-----|:-----|:-----|:-----|
| ann_return | 52.95% | 75.62% | ↑ 期望提升 |
| win_rate | 53.9% | - | 45-55% (已达) |
| sharpe | 2.511 | - | 保持 |
| mdd | -16.28% | - | 控制 |
| total_buys | 278 | 1188 | ↑ 预期增加 |
| TP exits PnL | 95.7% | - | 保持高质量退出 |

### 替代方案
- **B) MAX_POSITIONS=10**: R7 使用过，导致 over-expansion（R7 崩盘）。不考虑。
- **C) MAX_POSITIONS=7**: 保守过渡，但 R5 已证明 8 可行（+75.62%）。采纳 A 直接跳到 8。
- **D) 同时调整其他参数**: 任务明确要求"仅 1 个参数变更"，禁止。

### 风险分析
| 风险 | 严重度 | 缓解措施 |
|:-----|:-------|:---------|
| 仓位分散导致个股权重下降 | 🟡 MED | R5 已用 8 仓+75.62%，权重稀释可控。Dynamic sizing (line 604) 有 4% floor |
| 容量不足（候选股票不够 8 只） | 🟢 LOW | `min(MAX_POSITIONS, len(candidates))` 自动降级 |
| 回测结果低于 R8-B 52.95% | 🟡 MED | 更多仓位可能引入平庸持仓，但上限由 R5 的 75.62% 锚定 |
| Filter 路径下游候选不足 | 🟢 LOW | R8-B filter 链已验证通过率高，放宽仓位不会空心 |
| 与 R8-B 基准比较丢失 | 🟢 LOW | backtest_B.csv 已保存，可对比 |

### 理论依据
- R5: MAX_POSITIONS=8, ann_return=75.62%, 1188 trades — 证明 8 仓可行
- R8-B: MAX_POSITIONS=6, ann_return=52.95%, 278 buys — 仓位上限是吞吐量瓶颈
- 假设: 放宽仓位上限后，更多高质量信号可以被捕获（R8-B 的过滤质量已验证）
- 预期: buy 数从 278 提升到 350-400，年化收益在 55-75% 区间

---

## 变更清单

| # | 位置 (line) | 变更内容 | 类型 | 状态 |
|:--|:-----|:-----|:-----|:--|
| 1 | 67 | `MAX_POSITIONS = 6` → `8` | **参数** | ☐ |
| 2 | 2 | `Strategy B-R8` → `B-R9` | 文档 | ☐ |
| 3 | 10 | `MAX_POSITIONS RESTORED 6` → `EXPANDED 8` | 文档 | ☐ |
| 4 | 33 | `nlargest(MAX_POSITIONS=6)` → `8` | 文档 | ☐ |
| 5 | 413 | `nlargest(MAX_POSITIONS=6)` → `8` | 文档 | ☐ |
| 6 | 1118 | `Strategy B-R8` → `B-R9` | 输出标签 | ☐ |

### 每项变更的详细说明

#### #1: MAX_POSITIONS 常量 (line 67)
```python
# Before:
MAX_POSITIONS = 6                 # R8-B: restored to R6 level (was 10 in R7, over-expansion)
# After:
MAX_POSITIONS = 8                 # R9-B: expanded from 6 to 8 (R5 level, +75.62% ann proven)
```

#### #2-6: 文档同步
- docstring 版本号从 R8 更新为 R9
- 所有注释中的 `MAX_POSITIONS=6` 更新为 `8`
- 打印输出标签更新

---

## 不变参数确认（R8-B → R9-B 完全保留）

| 参数 | 值 | 验证方法 |
|:-----|:---|:---------|
| LEVERAGE | 1.0 | test_leverage_unchanged |
| CANDLE_CONFIRM | False | test_candle_confirm_false |
| MOM_20D_MIN | 0.05 | test_mom_20d_min |
| SECTOR_HEAT_HARD_FILTER | 0.65 | test_sector_hard_filter |
| REBALANCE_DAYS | 3 | test_rebalance_days |
| STOP_LOSS | -0.12 | test_stop_loss |
| TP1_HALF_PCT | 0.08 | test_tp1_half |
| TP2_FULL_PCT | 0.18 | test_tp2_full |
| TRAILING_STOP_PCT | -0.05 | test_trailing_stop |
| DECLINE_QUANTILE | 0.18 | test_decline_quantile |
| VOL_RATIO_MIN | 1.0 | test_vol_ratio |
| 其他全部参数 | R8-B 原值 | test_r8b_iron_rules_hold |

---

## TDD 计划

### 测试文件: test_r9b_tdd.py
基于 R8-B 测试结构，新增 R9-B 特定测试。

**RED 阶段** (编码前):
- `test_max_positions_expanded`: assert MAX_POSITIONS == 8 → 当前=6，预期 FAIL (RED)
- R8-B 铁律回归测试迁移: 所有其他 15+ 参数不变 → GREEN

**GREEN 阶段** (编码后):
- 修改 MAX_POSITIONS → test_max_positions_expanded 变 GREEN
- 所有其他测试保持 GREEN

### 命令行测试
```bash
cd /home/pebynn/quant/evo_optimizer
python -m pytest test_r9b_tdd.py -v
```

### 回测验证
```bash
cd /home/pebynn/quant/evo_optimizer
python strategy_reversal.py --source sina 2>&1 | tee backtest_r9b.log
```

提取 BACKTEST_SUMMARY 行，对比 R8-B 基线:
```python
# R8-B 基线: ann=0.5295|sharpe=2.511|mdd=-0.1628|wr=53.9|buys=278
# R9-B 期望: ann > 0.5295|buys > 278|wr 45-55%
```

---

## 自检清单
- [ ] Step 1 ✅ brainstorming → plan 文件已有设计方案节
- [ ] Step 2 ✅ writing-plans → plan ≥100 行
- [ ] Step 3 ☐ TDD → 测试 RED (MAX_POSITIONS=6 vs expected 8)
- [ ] Step 4 ☐ 编码 → MAX_POSITIONS 6→8 + 文档更新
- [ ] Step 5 ☐ debugging → 回测运行，检查输出
- [ ] Step 6 ☐ code-review → 自审通过
- [ ] Step 7 ☐ verification → 回测结果达标

---

## 回滚计划
如有问题，恢复 `MAX_POSITIONS = 6` 即可回到 R8-B 状态。所有其他参数不变，回滚成本为零。
