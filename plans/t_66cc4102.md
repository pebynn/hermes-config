# Implementation Plan: R1-代码优化-三策略改进
Task: t_66cc4102
Date: 2026-05-13
Status: IN PROGRESS

## 1. 设计方案

### 方案选择: 逐策略修复 + fallback_resolver 集成

**选择理由:**
- fallback_resolver.py 已存在且经过测试，提供 MySQL→Sina 自动回退
- 每个策略独立修复，降低级联风险
- 先修复基础问题(周期/路径)，再优化信号逻辑

**替代方案:**
- 方案B: 重建统一 Strategy 基类 — 拒绝：改动范围太大，风险高
- 方案C: 仅改路径和周期，不改信号逻辑 — 拒绝：C策略的未来函数必须修复

**风险:**
- MySQL 连接可能不可用 → fallback_resolver 自动回退 Sina
- C策略 buy2 窗口修复可能导致信号密度下降 → 用更多持仓补位
- B策略加回趋势过滤可能进一步降低信号数 → 放宽 decline 预过滤为 -0.035

### 2. 实施计划

### Phase 1: 基础设施修复（影响所有3个策略）

#### 1.1 策略A (strategy_momentum.py)
- [ ] 替换数据加载为 fallback_resolver.load_kline()
- [ ] START_DATE → "2025-05-01"
- [ ] OUTPUT_FILE → "/home/pebynn/quant/evo_optimizer/backtest_momentum.csv"
- [ ] TOP_N: 5→8 (任务要求8-10)
- [ ] 优化 FACTOR_WEIGHTS: 增加 ret_60d_z 权重 (0.20→0.28)，减少 ret_5d_z (0.08→0.05)
- [ ] 添加 ret_20d_vol_z 因子 (动量稳定性)
- [ ] 保留现有的T-1因子排名逻辑（已验证无未来函数）

#### 1.2 策略B (strategy_reversal.py)
- [ ] 替换数据加载为 fallback_resolver.load_kline()
- [ ] START_DATE → "2025-05-01"
- [ ] OUTPUT_FILE → "/home/pebynn/quant/evo_optimizer/backtest_reversal.csv"
- [ ] TREND_SCORE_ENABLED → True（加回趋势过滤）
- [ ] DECLINE_PREFILTER: -0.04 → -0.035（放宽以补偿趋势过滤）
- [ ] 修复 trades/nav 输出路径 → evo_optimizer/ 下
- [ ] KEEP_LOSERS → False, HOLD_WINNERS → True（保守化）

#### 1.3 策略C (strategy_chan.py)
- [ ] 替换数据加载为 fallback_resolver.load_kline()
- [ ] START_DATE → "2025-05-01"
- [ ] OUTPUT_FILE → "/home/pebynn/quant/evo_optimizer/backtest_chan.csv"
- [ ] **修复未来函数 #1**: end_dt 改为 `max(rd_dt, pd.Timestamp(max(backtest_dates, key=lambda d: pd.Timestamp(d) if pd.Timestamp(d) <= rd_dt else "2000-01-01")))` — 仅扫描到 rebalance date
- [ ] **修复未来函数 #2**: entry_price 保持 close（作为信号确认），但实际入场在次日开盘 → 改为: 信号日在T日，入场在T日之后的第一个可用交易日

## 3. TDD测试计划

测试用例:
1. test_leverage_is_one: 三个策略 LEVERAGE == 1.0
2. test_output_paths: 所有 OUTPUT_FILE 在 /home/pebynn/quant/evo_optimizer/ 下
3. test_backtest_period: START_DATE == "2025-05-01", END_DATE == "2026-04-30"
4. test_no_future_function_chan: Strategy C 不使用未来数据
5. test_fallback_resolver_used: 每个策略 import fallback_resolver
6. test_syntax: python3 -m py_compile 通过所有文件

## 4. 编码变更清单

### strategy_momentum.py
```
- line 15: START_DATE = "2025-01-01" → "2025-05-01"
- line 19: TOP_N = 5 → 8
- line 33: OUTPUT_FILE → "/home/pebynn/quant/evo_optimizer/backtest_momentum.csv"
- line 36: FACTOR_WEIGHTS: ret_60d_z:0.20→0.28, ret_5d_z:0.08→0.05
- lines 108-130: 替换 load_data_mysql()/load_data_sina() → fallback_resolver.load_kline()
- 添加: from fallback_resolver import load_kline
```

### strategy_reversal.py
```
- line 51: START_DATE = "2025-01-01" → "2025-05-01"
- line 60: OUTPUT_FILE → "/home/pebynn/quant/evo_optimizer/backtest_reversal.csv"
- line 70: TREND_SCORE_ENABLED = False → True
- line 75: DECLINE_PREFILTER = -0.04 → -0.035
- line 82: KEEP_LOSERS = True → False
- line 83: HOLD_WINNERS = False → True
- lines 624-629: 输出路径 → evo_optimizer/
- lines 320-373: 数据加载 → fallback_resolver.load_kline()
- 添加: from fallback_resolver import load_kline
```

### strategy_chan.py
```
- line 47: START_DATE = "2025-01-01" → "2025-05-01"
- line 61: OUTPUT_FILE → "/home/pebynn/quant/evo_optimizer/backtest_chan.csv"
- line 403: end_dt = rd_dt + timedelta(days=2) → end_dt = rd_dt
- lines 209-257: 数据加载 → fallback_resolver.load_kline()
- 添加: from fallback_resolver import load_kline
```

## 5. 调试策略

- 语法检查: python3 -m py_compile
- 快速导入测试: python3 -c "from strategy_xxx import *"
- LEVERAGE确认: grep LEVERAGE strategy_*.py
- 路径确认: grep OUTPUT_FILE strategy_*.py

## 6. 验证

- [ ] python3 -m py_compile strategy_momentum.py
- [ ] python3 -m py_compile strategy_reversal.py
- [ ] python3 -m py_compile strategy_chan.py
- [ ] grep "LEVERAGE" → all = 1.0
- [ ] grep "OUTPUT_FILE" → all in evo_optimizer/
- [ ] grep "START_DATE" → all = "2025-05-01"
- [ ] Strategy C: end_dt <= rd_dt (no future look)
- [ ] 7步自检全部完成
