# R1-C-代码优化 实施计划

## Task ID: t_5e30f0f6
## 策略文件: /home/pebynn/quant/evo_optimizer/strategy_chan.py
## 回测输出: /home/pebynn/quant/evo_optimizer/backtest_C.csv

---

## 设计方案

### 方案选择: 逐项修复未来函数 + 参数调整

**替代方案**: 完全重写入场逻辑使用T-1信号 → 过于激进，与R1渐进优化目标不符
**风险**: 修复未来函数后年化回报可能从558%显著下降，需如实汇报

---

## 变更清单

### A. 未来函数修复 (P0 — 必须修)

#### A1: vol_ma20排除当日成交量
- **位置**: `precompute_indicators()` line 152
- **现状**: `vol_ma20 = _rolling_mean(vol_v, 20)` → vol_ma20[i]含vol[i]
- **修复**: 
  ```python
  vol_ma20 = pd.Series(vol_v).rolling(20, min_periods=20).mean().shift(1).values
  ```
- **影响**: `check_entry_conditions` (line 179), `detect_buy2_dates` (line 127) 自动修复
- **验证**: 检查vol_ma20[20] = mean(vol[0:19])，不含vol[20]

#### A2: 入场价改为次日开盘
- **位置**: main() entry logic, line 408
- **现状**: `open_at_i = df["开盘"].values[i]` — 同日开盘入场(含未来函数)
- **修复**: 
  ```python
  next_i = i + 1
  if next_i >= len(df): continue  # skip if no next day
  open_at_i = df["开盘"].values[next_i]
  ```
- **铁律依据**: "因子计算用T-1数据, 入场在次日开盘"
- **影响**: entries将推迟1天，可能减少交易机会

### B. 参数调整 (P1 — 任务要求)

| 参数 | 当前 | 目标 | 位置 |
|------|------|------|------|
| MAX_HOLDINGS | 8 | 10 | line 54 |
| MIN_HOLDINGS | 6 | 8 | line 55 |
| OUTPUT_FILE | backtest_chan.csv | backtest_C.csv | line 62 |

### C. 文档更新
- 文件头R23标记 → R1-C优化说明
- 添加FIXED注释标记未来函数修复位置

---

## 测试策略 (TDD)

### Test 1: vol_ma20不含量
- 构造模拟数据(20天vol=1.0, 第21天vol=100.0)
- 验证vol_ma20[21] = 1.0 (不含100.0的20日均值)

### Test 2: 入场价偏移
- 模拟bar序列，验证entry使用open[i+1]而非open[i]

### Test 3: 语法编译
- `python3 -m py_compile strategy_chan.py`

### Test 4: 完整回测运行
- `python3 strategy_chan.py --source auto`
- 验证产出文件存在、无NaN、指标合理

---

## 执行步骤
1. ✅ brainstorming (本文件)
2. ✅ writing-plans (本文件)
3. TDD — 写测试验证修复
4. 编码实现 — patch strategy_chan.py
5. debugging — 运行回测，检查错误
6. code-review — 自审所有修改
7. verification — 确认回测结果合理

---

## 预期影响
- 年化回报可能从558%下降（未来函数修复会暴露真实收益率）
- 胜率可能小幅下降（入场延迟1天错过部分机会）
- 持仓数增加(8-10) → 分散度提升，回撤可能下降
